import { ReleaseSprintBAcklog } from './release-sprint-backlog';

describe('ReleaseSprintBAcklog', () => {
  it('should create an instance', () => {
    expect(new ReleaseSprintBAcklog()).toBeTruthy();
  });
});
